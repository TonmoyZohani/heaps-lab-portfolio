import WorkPageClient from "@/components/work/WorkPageClient";

export const metadata = {
  title: "Work | Heaps Lab",
  description:
    "Portfolio and case studies — every project we ship gets an entry. Case studies go deeper, for projects worth a full problem-to-result walkthrough.",
};

export default function WorkPage() {
  return <WorkPageClient />;
}
